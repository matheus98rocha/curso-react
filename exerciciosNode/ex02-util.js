function upper(text) {
    return text.toUpperCase();
}

function lower(text) {
    return text.toLowerCase();
}

function plus(x, y) {
    let result
    result = x + y;
    return result
}

module.exports = { upper, lower, plus }
